/**
 * 1. Level 1：標準練習
 * 題目 1：迷宮最短路徑（只走 0）
 * 輸入：'0' 表示路，'1' 表示牆
 * 目標：從 (0,0) 走到 (n-1,m-1) 的最短步數
 * 檔案：Graph_General/Preview_Coordinate_System/Coordinate_System_01.cpp
 * 
 * 2. Level 2：增加新條件
 * 題目 2：迷宮最短路徑 + 起點不一定是 (0,0)
 * 輸入：
 * - 起點 (sx, sy)
 * - 終點 (tx, ty)
 * - 地圖仍是 '0' 路，'1' 牆
 * - 目標：從指定起點走到指定終點的最短步數
 * 
 * 題目 3：找出所有可達點的步數表
 * 輸入：
 * - 地圖：'0' 可走，'1' 是牆
 * - 起點 (x, y)
 * 輸出：
 * - 一個跟原始地圖一樣大小的整數矩陣，裡面每格代表從起點走到該格所需的步數（不可達則填 -1）
 * 
 * 3. Level 3：進階應用
 * 題目 4：找出所有島嶼的個數（LeetCode 200 類型）
 * 輸入：
 * - 地圖是 '1' 表示陸地，'0' 表示水
 * - 你可以上下左右走
 * 目標：
 * - 計算有幾個「島嶼」，也就是連在一起的 '1'
 * - 練習 BFS 多起點、多區域的判斷（需要跑 BFS 多次）
 * 
 * 題目 5：最短路徑但地圖中有陷阱 
 * 地圖：
 * - '0' 是正常地面
 * - '1' 是牆
 * - 'T' 是陷阱，你可以走但會多消耗 2 步（等於這格要花 3 步）
 * 
 * 目標：
 * - 從 (0, 0) 到 (n-1, m-1) 的 最短消耗步數
 * - ✅ 考你 BFS + 權重延伸（類似 Dijkstra 概念）
 * 
 * 題目 6：有兩個人從不同起點能不能相遇？
 * - 地圖仍是 '0' 可以走，'1' 是牆
 * - 一個人在 (x1, y1)，一個人在 (x2, y2)
 * - 每次兩人都可以各自向四周移動一步
 * 
 * 問：兩人是否能走到同一格（即是否有交會點）？
 * ✅ 雙向 BFS 入門題
 */